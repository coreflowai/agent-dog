{
  "name": "agent-dog",
  "version": "0.1.0",
  "description": "Real-time observability for AI agent sessions",
  "scripts": {
    "dev": "bun run --watch server.ts",
    "start": "bun run server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@socket.io/bun-engine": "^0.1.0",
    "drizzle-orm": "^0.39.0",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "@anthropic-ai/claude-code": "^2.1.42",
    "@types/bun": "^1.2.0",
    "socket.io-client": "^4.8.0"
  }
}
